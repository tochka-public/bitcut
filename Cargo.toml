[package]
name = "bitcut"
version = "1.0.1"
edition = "2021"
description = "Create and apply binary patches"
license = "MIT"
repository = "https://github.com/tochka-public/bitcut"
readme = "README.md"
categories = ["command-line-utilities", "compression"]
keywords = ["binary", "bin", "diff", "patch"]
exclude = ["/.github", "/target", "/fuzz", "/docs", "/demo-wasm"]

[workspace]
members = []
exclude = ["fuzz", "demo-wasm"]

[lib]
path = "src/lib.rs"

[[bin]]
name = "bitcut"
required-features = ["cli"]
path = "src/main.rs"

[dependencies]
memchr = "2.7"
rustc-hash = "2.1"
xxhash-rust = { version = "0.8", features = ["xxh3"] }

[dependencies.clap]
version = "4.5"
features = ["derive"]
optional = true

[dependencies.anyhow]
version = "1.0"
optional = true

[features]
default = ["cli"]
cli = ["clap", "anyhow"]
# Matcher instrumentation for the algorithm walkthrough. Off by default: it
# adds a parameter checker and, later, the trace recorder, none of which the
# library itself uses.
demo = []

[dev-dependencies]
criterion = { version = "0.5", features = ["html_reports"] }
rstest = "0"
proptest = "1"
insta = "1.47.2"
zstd = "0.13"

[[bench]]
name = "my_benchmark"
harness = false
test = false

[lints.rust]
warnings = "deny"

[lints.clippy]
all = { level = "deny", priority = -1 }
pedantic = { level = "deny", priority = -1 }
cargo = { level = "deny", priority = -1 }
unwrap_used = "deny"
expect_used = "deny"
panic = "deny"
unreachable = "deny"
indexing_slicing = "deny"
string_slice = "deny"
arithmetic_side_effects = "deny"
cast_possible_truncation = "deny"
cast_sign_loss = "deny"
cast_possible_wrap = "deny"
cast_precision_loss = "deny"
float_cmp = "deny"
lossy_float_literal = "deny"
as_conversions = "deny"
default_numeric_fallback = "deny"
